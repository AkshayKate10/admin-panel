import * as _ from "./Actions";

const initialstate = {
  allRows: [],
  filteredRows: [],
  currentFilter: {},
};

function reducer(state = initialstate, action) {
  switch (action.type) {
    default:
      return state;
  }
}
export { reducer };
